import React from 'react';
import './search-panel.css';

const SearchPanel: React.FC = () => (
  <input type="text" placeholder="Search by items" className="form-control search-input" />
);

export default SearchPanel;
